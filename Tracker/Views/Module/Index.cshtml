@model IEnumerable<Tracker.Models.Module>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ModCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Level)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Credits)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ModCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Level)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Credits)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ModuleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ModuleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ModuleID })
        </td>
    </tr>
}

</table>
